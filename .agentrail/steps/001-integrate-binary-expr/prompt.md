Extend snobol4/src/emit_asm.sno KASN to handle: (a) X = Y (variable copy: la r0,_V_Y / lw r0,0(r0) / store to _V_X). (b) X = T1 OP T2 where T1/T2 are each digit-run literals OR alpha-run var refs, and OP is + - or *. Codegen: load T1 to r0; push r0; load T2 to r0; mov r1,r0; pop r0; add/sub/mul r0,r1; la r1,_V_X; sw r0,0(r1). Dispatch order in KASN: try binary first, then int literal (m5 existing), then var copy. Watch dcsno ~233-stmt cap (currently 176, ~30 budget). Demo: examples/add.f -> '20'. Regress hello.f, print-int.f, print-var.f.