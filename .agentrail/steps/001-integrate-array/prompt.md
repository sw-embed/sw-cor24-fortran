Add DIMENSION + array access to snobol4/src/emit_asm.sno. (1) KDIM parses 'DIMENSION A(N)' and reserves N words as '_V_A: .zero <3*N>' in VARDATA. (2) Refactor EXPR: add EXPRA path for array refs '<VAR>(<IDX>)' that computes addr via runtime _aindex(base, idx) helper and dereferences. (3) Add EXPRDP MODE='P' that emits putint+newline; route PRINT *, <expr> through EXPR with MODE='P', collapsing KPRTI/KPRTV/array-PRINT. (4) Add KASN array-LHS form '<VAR>(<IDX>) = <expr>'; routes through EXPR with MODE='AA' that stores r0 into &<VAR>(<IDX>). (5) Add _aindex helper to snobol4/runtime/prelude.s. Demo: examples/array1.f -> '30' (A(3)=3*10). Regress all 6 prior demos. Watch ~12K source-byte cap.