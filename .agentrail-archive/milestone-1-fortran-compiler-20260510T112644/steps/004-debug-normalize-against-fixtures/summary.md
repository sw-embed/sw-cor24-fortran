normalize.sno passes 8/8 fixtures via scripts/test-normalize.sh. SNOBOL4-implemented first compiler phase running real on cor24-emu, reading fixed-form .f from 0x090000, emitting structured stmt<N> records. Used Path Y (extract function-call results to temp variables before arithmetic / nested use) to work around the dcsno-funcall-arithmetic bug; sites marked with XXX comments for easy revert when dcsno ships.