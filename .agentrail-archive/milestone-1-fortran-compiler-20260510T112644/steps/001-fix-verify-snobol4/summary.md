verify-snobol4.sh rewritten to use canonical 'snobol4 --load-binary <prog>@0x080000 --entry 0 --quiet' invocation. Filter 'Loaded N bytes ...' lines off stdout (small cor24-emu --quiet inconsistency worth a follow-up brief). Verified: exit 0 with 'OK -- SIZE/SUBSTR/CHAR behave as expected'. The SNOBOL4 dogfood loop is now real -- our verification fixture, our test runner, the deployed interpreter, all canonical.