Per tools/briefs/dcftn-emit-asm-pattern-anchoring.md option B. Add inline identifier validation after each name capture in snobol4/src/emit_asm.sno: VN, SRC, T1, T2, T1IDX, ARVN, ARIDX (for non-digit branches). Validation form: 'X SPAN(literal-alpha-digit-class) . M :F(BAD); EQ(SIZE(M), SIZE(X)) :F(BAD)'. Note: SPAN can't take a variable class -- inline the literal. Regress all 7 demos; add malformed-input fixtures producing ; WARN:.