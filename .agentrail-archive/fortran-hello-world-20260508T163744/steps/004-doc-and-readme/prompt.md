Add a 'Live demo' section to README.md pointing at examples/hello.f + examples/hello.s + examples/hello.lgo and the Path A status. Note that the full FTI-0 compiler is a parallel effort. Update docs/snobol4-blockers.md briefly to note that snobol4.lgo is now deployed (the original primary blocker has cleared) and the live blocker has shifted (dcemu --lgo+--uart-file bug + FTI-0 compiler not yet implemented). markdown-checker must pass.