Extend snobol4/src/emit_asm.sno for FTI-0 labels + GOTO + IF-GOTO. (1) Update LOOP dispatch to capture 'label=' field; emit 'L<N>:' before the per-kind emit when label present. (2) Add KGOT (kind=GOTO) -> bra L<N>. (3) Add KIFG (kind=IF_GOTO) -> parse 'IF (<expr>) GOTO <N>', eval expr to r0, emit ceq r0,z + brf L<N>. (4) Refactor KASN's expr-eval into a shared EXPR block; KASN/KIFG set MODE='A'/'I' and a target before jumping to EXPR. Demo: examples/goto1.f -> '1\n2\n3\n4\n5'. Regressions: hello.f, print-int.f, print-var.f, add.f.